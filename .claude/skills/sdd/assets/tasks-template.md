# Tareas — Spec NNN

Detalle de diseño en `plan.md`. Orden de dependencia; `<comando de tests>` en
verde al cerrar cada una.

- [ ] T1. <Andamiaje: proyecto, dependencias, runner de tests.> (RF: —)
      Hecho cuando: <comprobación concreta que se puede ejecutar>.
- [ ] T2. <Unidad pequeña del núcleo.> (RF-n.x, RF-m)
      Hecho cuando: <los casos concretos que quedan en verde>.
- [ ] T3. <...> (RF-n)
      Hecho cuando: <...>.
- [ ] Tn. Cierre: casos límite transversales, demo manual y repaso de la spec.
      (Todos)
      Hecho cuando: cada criterio EARS y caso límite tiene test, y la demo manual
      corre sin errores.
